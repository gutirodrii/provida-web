---
const { titulo, descripcion, bgcolor } = Astro.props
---

<div
  class={`${bgcolor ? "bg-" + bgcolor : "bg-slate-200"} p-6 rounded-lg shadow-md`}
>
  <h3 class="text-xl font-semibold text-gray-800 mb-2">{titulo}</h3>
  <p class="text-gray-600">{descripcion}</p>
</div>
